1) Which of the following is correct syntax for a button click event handler, foo?
(2 Points)

<button onclick=this.foo()>

<button onclick=this.foo>

<button onClick={this.foo()}>

<button onClick={foo}>



2) What happens when the following render() method executes?<br /><br /> render(){<br /> let langs = ["Ruby","ES6","Scala"]<br /> return (<div><br /> {langs.map(it => <p>{it}</p>)}<br /> </div>)<br /> }<br />
(2 Points)

Displays the list of languages in the array

Error. Cannot use direct JavaScript code in JSX

Displays nothing

Error. Should be replaced with a for..loop for correct output

3)What is the Output?const [data , setData] = useState("Alex");console.log(data);
(2 Points)

data

data Alex

Alex

error

4) Consider the below function – which is the correct syntax of arrow function?Msg = function() {return "Good Morning";}
(2 Points)

Msg = () => {return "Good Morning";}

Msg = () => "Good Morning";

Both A. and B.

None of the above

5)Select correct option to get output as below: 

React is imp test.
(2 Points)

<p>React is<em>imp</em> test.</p>

<p><strong>React is imp test.</strong></p>

<p><i>React is imp test.</i></p>

<p>React is<strong>imp</strong> test.</p>

6)Choose the correct style example of class selector:
(2 Points)

"p { text-align: center; color: red; }"

"#para1 {text-align: center;color: red;}"

".center {text-align: center;color: red;}"

"h1, h2, p {text-align: center;color: red;}

7)Consider below form code for reference: 

Which of the below script code is correct to display error msg in alert box if fname value for Name will not be entered?

<form name="myForm" action="/next_page.php" onsubmit="return validateForm()" method="post">
  Name: <input type="text" name="fname">
  <input type="submit" value="Submit">
</form>
(2 Points)

"<script>function validateForm(){var name = document.forms[""myForm""][""fname""].value;if (name == """") {alert(""Name must be provided"");return false;}}</script>"

"<script>function validateForm(){var name = document.forms[""myForm""][""fname""].value;if (name != """") {alert(""Name must be provided"");return false;}}</script>"

"<script>function validateForm(){var name = document.forms[""myForm""][""fname""].value;if (name == """") {alert(""Name must be provided"");return false;}}</script>"

"<script>function validateForm(){var name = document.forms[""myForm""][""fname""].value;if (name == null) {alert(""Name must be provided"");return false;}}</script>"

8)State true/false:

Below code snippet will display the heading 'BootStrap' at Medium Mode in bootstrap:

<div class="visible-md"><h1>BootStrap</h1></div>
(2 Points)

TRUE

FALSE

9) class Sub extends Base{
   constructor()
{
    super("Anju"); //Line 1 
   }

Refer to the above code of a derived class, if there is no constructor defined in the Base class, 
what will be the suggestion for the code?
(2 Points)

super() will call the base class constructor, if it is not there it will give error.

There is no issue in the code. It will work fine.

In place of super("Anju"), we should write super() to execute the code

We sould delete the line 1 to execute the code.

10) et arrFun = (num1,num2) => num1.length+ num2;
 console.log(arrFun("India",5)); 

Refer to the above code snipped, what will get printed in console of browser?
(2 Points)

India5

10

6

NaN

11) __________ are the pure functions which takes the previous state and an action, and return the next state.
(2 Points)

Store

Action

Reducer

Subscribe


12) What happens when the following render() method executes?

render()
{
   let langs = ["Ruby","ES6","Scala"]
     return (<div>
       {langs.map(it => <p>{it}</p>)}
     </div>)
 }
(2 Points)

Displays the list of languages in the array

Error. Cannot use direct JavaScript code in JSX

Displays Nothing

Error. Should be replaced with a for..loop for correct output


13) In JSX, How do you write an inline style specifying the font-size:12px and color:red?
(2 Points)

style={{font-size:12,color:'red'}}

style={{fontSize:'12px',color:'red'}}

style={fontSize:'12px',color:'red'}

style={{font-size:12px,color:'red'}}

14)
Consider the below code:

var Helloword=(props)=>
{
 return(
 <div>
 hello world 1
 </div>
 <div>
 Hello world 2
 </div>
 );
}
ReactDOM.render(<Helloworld/>,mountNode)

What will be the output?
(2 Points)

Hello world 1

Hello world 2

Hello world 1 Hello world 2

Error

















