// Assuming you have PostService.spec.ts and Post.ts

import { TestBed, async } from '@angular/core/testing';
import { PostsComponent } from './posts.component';
import { PostService } from '../post.service'; // Import PostService
import { Post } from './post'; // Import Post model

describe('PostsComponent', () => {
  let component: PostsComponent;
  let fixture: TestBed.createComponent<PostsComponent>;
  let postService: PostService;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PostsComponent ],
      providers: [ PostService ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PostsComponent);
    component = fixture.componentInstance;
    postService = TestBed.inject(PostService);
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });

  // Add test cases for component behavior here:
  // * test getPosts functionality (data fetching)
  // * test display of post data in template
  // * (optional) test button click behavior
});
